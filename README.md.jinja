# {{ project_name }}

Welcome to **{{ project_name }}** — a Python project scaffolded with [Copier](https://copier.readthedocs.io/).

---

## 🛠 Project Setup

1. Create a virtual environment:

    ```bash
    python -m venv .venv
    source .venv/bin/activate  # or .venv\Scripts\activate on Windows
    ```

2. Install dependencies:

    ```bash
    pip install -r requirements.txt  # if present
    ```

3. Run the app:

    ```bash
    python -m {{ project_slug }}
    ```

---

## 📦 Project Structure

```text
{{ project_slug }}/        # Main application code
tests/                    # Unit tests
README.md                 # This file
pyproject.toml            # Project metadata and dependencies
```
## 🚀 Getting Started

Modify `main.py` to begin building your project.
